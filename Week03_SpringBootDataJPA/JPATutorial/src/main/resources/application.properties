spring.application.name=JPATutorial

#Connecting to datasource
spring.datasource.url=jdbc:mysql://localhost:3306/sachin?useSSL=false
spring.datasource.username=root
spring.datasource.password=MySQL@45

#(update) it will hibernate that use previous table and for first only create new table for entity
spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:data.sql

